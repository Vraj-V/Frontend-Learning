## Module Pattern

The Module Pattern encapsulates private data and exposes public methods.

### Why use it?
- Data privacy
- Organized code
- Avoids global pollution

### Example
`users` array is private, only accessible via `addUser`.
