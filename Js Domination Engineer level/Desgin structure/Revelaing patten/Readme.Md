## Revealing Module Pattern

A variation of Module Pattern where methods are explicitly exposed.

### Why use it?
- Clear public API
- Better readability
- Easy maintenance

### Example
Only `increment` and `reset` are accessible.
