## IIFE Pattern

Immediately Invoked Function Expression runs as soon as it is defined.

### Why use it?
- Avoid global variables
- Create private scope

### Example
The `count` variable is private and cannot be accessed outside.
